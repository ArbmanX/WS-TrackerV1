APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost
# SUDO_ADMIN_EMAIL=
# SUDO_ADMIN_PASSWORD=


APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=ws_trackerv1
DB_USERNAME=arbman
DB_PASSWORD=Persistent@247!

# WorkStudio Cache TTLs (seconds) — override per-dataset defaults
WS_CACHE_TTL_SYSTEM_WIDE_METRICS=86400
WS_CACHE_TTL_REGIONAL_METRICS   =86400
WS_CACHE_TTL_DAILY_ACTIVITIES   =86400
WS_CACHE_TTL_ACTIVE_ASSESSMENTS =86400
WS_CACHE_TTL_JOB_GUIDS          =86400

 
WORKSTUDIO_BASE_URL=https://ppl02.geodigital.com:8372/DDOProtocol/
WORKSTUDIO_TIMEOUT=60
WORKSTUDIO_CONNECT_TIMEOUT=10
WORKSTUDIO_SERVICE_USERNAME=ASPLUNDH\cnewcombe
WORKSTUDIO_SERVICE_PASSWORD=chrism


SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

# WorkStudio Cache TTLs (seconds) — override per-dataset defaults
# WS_CACHE_TTL_SYSTEM_WIDE_METRICS=900
# WS_CACHE_TTL_REGIONAL_METRICS=900
# WS_CACHE_TTL_DAILY_ACTIVITIES=1800
# WS_CACHE_TTL_ACTIVE_ASSESSMENTS=600
# WS_CACHE_TTL_JOB_GUIDS=3600

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
