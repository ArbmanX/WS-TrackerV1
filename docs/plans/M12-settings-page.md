# M12: Settings Page

> Depends on: M1
> Unlocks: M13

## Goal

User settings page at `/settings` with profile and preferences sub-pages.

## Micro Tasks

- [ ] Settings hub layout (or simple tabbed page) at `/settings`
- [ ] Profile section — display name, email, WS username (read-only where appropriate)
- [ ] Preferences section — theme selector (reuse onboarding theme component), default landing view picker
- [ ] `default_landing` column on `user_settings` table (migration)
- [ ] Password change link (uses existing Fortify flow)
- [ ] Bottom-pinned Settings link in sidebar routes here
